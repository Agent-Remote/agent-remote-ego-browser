# Learning bundle examples

`unsigned-learning-bundle` intentionally contains payload files only. It has no
`manifest.json` and no signature, so the Bridge will reject it. The fixture is
for deterministic manifest-tooling tests and must not be installed or cited as
production release evidence.
